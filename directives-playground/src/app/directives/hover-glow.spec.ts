import { HoverGlowDirective } from './hover-glow';

describe('HoverGlowDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverGlowDirective();
    expect(directive).toBeTruthy();
  });
});
