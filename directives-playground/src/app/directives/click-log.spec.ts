import { ClickLogDirective } from './click-log';

describe('ClickLogDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickLogDirective();
    expect(directive).toBeTruthy();
  });
});
